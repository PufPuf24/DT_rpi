# Battery Digital Twin -- runtime dependencies (Windows and Raspberry Pi/Linux both work,
# see BatteryMonitorGUI.py's module docstring). Versions are loose minimums on purpose:
# on Raspberry Pi OS, pip resolves ARM wheels from piwheels.org, and pinning to a version
# newer than what piwheels has built yet would force a from-source build.
pyserial>=3.5
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
customtkinter>=5.2

# SoH degradation estimation (soh/, battlib/) -- see soh/README.md. Not yet
# imported by the Maintenance cycle (Phase 1, wired into BatteryMonitorGUI.py);
# required once the opportunistic FFNN estimator (Phase 2) loads
# soh/deployment_package.joblib (needs pandas/scikit-learn to unpickle) and
# gates candidates through soh/online.py (needs pandas).
scikit-learn>=1.3
joblib>=1.3
pandas>=2.0
